{% extends 'base.html.twig' %}

{% block title %}{{ 'title.survey_create'|trans }}{% endblock %}

{% block body %}
    <div class="content">
        <h1>{{ 'title.survey_create'|trans }}</h1>
        {{ form_start(form, { method: 'post', action: url('survey_create') }) }}
        {{ form_widget(form) }}
        <br>
        <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-success" />
        {{ form_end(form) }}
        <br><br><br><br>
        <a class="btn btn-outline-dark" href="{{ url('surveys_index') }}"
           title="{{ 'label.back_to_mainpage'|trans }}">{{ 'label.back_to_mainpage'|trans }}</a>
    </div>
{% endblock %}