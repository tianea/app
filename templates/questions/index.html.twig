{% extends 'base.html.twig' %}


{% block title %}
    {{ 'label.questions_index'|trans }}
{% endblock %}

{% block body %}
    <div class="content">
    <h1>{{ 'label.questions_index_view'|trans }} {{ survey.name }}</h1>
        <ol>
            {% for row in questions %}
                <li>
                    {{ row.content }}
                </li>
                <br>
            {% endfor %}
        </ol>
       <a class="btn btn-info" href="{{ url('questions_add', {'id': survey.id}) }}" title="{{ 'title.questions_add'|trans }}">
            {{ 'title.questions_add'|trans }}
        </a>
        <a class="btn btn-info" href="{{ url('questions_edit', {'id': survey.id}) }}" title="{{ 'title.questions_edit'|trans }}">
            {{ 'title.questions_edit'|trans }}
        </a>
        <br><br><br>
        <a class="btn btn-outline-primary" href="{{ url('surveys_view', {'id': survey.id}) }}"
           title="{{ 'label.back_to_survey' }}">
            {{ 'label.back_to_survey' }}
        </a>
        <br><br><br>
    </div>
{% endblock %}